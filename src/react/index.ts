/**
 * React bindings — import via `@clearstorm/contact-form/react`.
 *
 * ```tsx
 * import { ContactForm } from "@clearstorm/contact-form/react";
 * import "@clearstorm/contact-form/styles.css";
 *
 * <ContactForm form={spec} config={{ endpoint }} />
 * ```
 */
export { ContactForm, Field, type ContactFormProps, type FieldProps } from "./ContactForm";